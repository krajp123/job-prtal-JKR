# Runs on a different port than the public frontend (3000) so they never collide locally.
# In production this whole app is built and deployed to admin.yourdomain.com,
# completely separate from the public site's deployment.
VITE_ADMIN_API_BASE_URL=http://localhost:5000/admin-api
